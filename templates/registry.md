# Change Registry

<!-- One row per change: experiments (EXP-NNNN) and verifier changes
     (VER-NNNN). Allocate ids as max existing + 1 per prefix, zero-padded to
     4 digits. Update the row at every status change. Keep rows sorted by id. -->

| ID | Slug | Type | Parent | Status | Iter | Headline metric | Date |
|----|------|------|--------|--------|------|-----------------|------|
